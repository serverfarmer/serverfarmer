#!/bin/sh

# Github base path, from which to install extensions. You can replace it with your
# own Github user, but first you need to fork ALL Server Farmer repositories, or
# at least all that you want to use, or that are used as dependencies - and then
# maintain your forked versions.
#
# If is strongly recommended to leave this setting as-is, unless you really know,
# what you're doing (eg. fork of the entire Server Farmer project).


echo "https://github.com/serverfarmer"
